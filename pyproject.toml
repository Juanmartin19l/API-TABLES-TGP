[project]
name = "api-tables-tgp"
version = "0.1.0"
description = "API para procesar archivos OCR financieros"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "python-multipart",
    "pydantic",
]
